# `App\Services`

Application use-case services live here. Services own business rules and
business transaction boundaries, depend on repository contracts for
persistence, and do not read HTTP requests or return HTTP responses.

Do not add a generic `CommonService`.
