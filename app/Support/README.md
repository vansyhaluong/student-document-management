# `App\Support`

Small, cohesive application infrastructure shared by multiple layers lives
here, such as the approved response, request-trace, or clock abstractions.

This namespace is not a miscellaneous helper bucket. Business use cases belong
in `App\Services`, and persistence belongs in `App\Repositories`.
