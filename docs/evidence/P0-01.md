# P0-01 — Runtime baseline evidence

- Task: `P0-01`
- Date: 2026-07-30
- Branch: `chore/p0-01-environment-audit`
- Status: **PASS**

## Scope

This evidence records the development runtime baseline and the user-scope
environment smoke test required before P0-02. It does not scaffold Laravel or
create `package.json`.

## Approved baseline

| Component | Baseline | Observed | Result |
|---|---|---|---|
| Laravel | 13.x | Packagist metadata exposes Laravel 13.x and requires PHP `^8.3` | PASS |
| PHP | 8.4 | Herd PHP `8.4.23` | PASS |
| Composer | `2.10.2` | `2.10.2` from User PATH | PASS |
| Node.js | `24.16.0` | `v24.16.0` | PASS |
| npm | `11.13.0` | `11.13.0` | PASS |

## PHP and Composer

`where.exe php` resolves Herd first. `php --ini` confirms the active
configuration file is:

```text
C:\Users\admin\.config\herd\bin\php84\php.ini
```

The active Herd runtime already had the required configuration, so its
`php.ini` was not modified. A fresh PowerShell process verified these modules:

| Module | Result |
|---|---|
| `PDO` | PASS |
| `pdo_mysql` | PASS |
| `intl` | PASS |
| `mbstring` | PASS |
| `openssl` | PASS |
| `fileinfo` | PASS |
| `curl` | PASS |
| `libxml`, `SimpleXML`, `xml`, `xmlreader`, `xmlwriter` | PASS |

Composer `2.10.2` is exposed through the user-scope launcher at
`C:\Users\admin\AppData\Local\Composer\composer.bat`. Its directory is the
first entry in User PATH. System PATH was not changed. `composer diagnose`
passed platform, Git, Packagist HTTP/HTTPS, GitHub rate-limit, key, version,
security-advisory, and disk checks.

The diagnostic reported the PHP zip extension as present; standalone `unzip`
and 7-Zip were unavailable. This is informational and is not a P0-01 failure.

## Node.js and npm

The approved runtime baseline is:

```text
Node.js 24.16.0
npm 11.13.0
```

`.nvmrc` pins Node.js `24.16.0`. P0-01 intentionally does not create an empty
`package.json`. P1-01 must apply the approved versions through `engines` and
`packageManager`; CI must use these exact versions without selecting them again.

## Commands actually run

The final smoke test ran in a new `powershell.exe -NoProfile` process after
reloading Machine and User PATH:

```powershell
where.exe php
php -v
php --ini
php -m
where.exe composer
composer --version
composer diagnose --no-interaction
composer show laravel/framework '13.*' --all --format=json
where.exe node
node --version
where.exe npm
npm --version
Get-Content .nvmrc
git diff --check
git status --short
```

## Repository changes

- `.nvmrc` records Node.js `24.16.0`.
- `docs/IMPLEMENTATION_PLAN.md` distinguishes baseline approval in P0-01 from
  applying `engines` and `packageManager` during P1-01.
- No `package.json`, `php.ini`, `.env`, private SQL, or machine-specific import
  configuration is committed by this task.

## Conclusion

All P0-01 acceptance conditions are satisfied: active PHP and extensions,
Composer through User PATH, Laravel 13.x/PHP compatibility, Node/npm baseline,
and `.nvmrc` all passed. P0-01 is complete; this does not satisfy P0-02 through
P0-04 and does not open P1.
