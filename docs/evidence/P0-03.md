# P0-03 — Sanitized schema validation evidence

- Task: `P0-03`
- Date: 2026-07-30
- Branch: `chore/p0-03-sanitized-schema-validation`
- Status: **PASS**

## Scope

This evidence records the database-agnostic sanitized baseline, its clean
import into the dedicated MariaDB 10.11 test database, and verification of the
required schema metadata and database-enforced invariants. It does not define
the private import policy assigned to P0-04 and does not create Laravel
migrations.

## Inputs and data safety

The private source remains the local file:

```text
student_document_management.sql
```

The file is excluded by the existing `*.sql` ignore rule, is not tracked by
Git, and was not modified. Its SHA-256 before and after P0-03 validation was:

```text
2039D1960C328E52849A6154DFE0DE1A4C94E3E7E0B580213A659E228124B2FF
```

Safe statement-level inspection found 12 private `INSERT` statements targeting
`students` and `document_types`. No private row values were printed, copied
into evidence, or imported into the test database.

The version-controlled sanitized baseline contains:

| Metadata | Private source | Sanitized baseline | Result |
|---|---:|---:|---|
| Tables | 5 | 5 | PASS |
| `ALTER TABLE` statements | 11 | 11 | PASS |
| Triggers | 1 | 1 | PASS |
| FK/CHECK constraints | 8 | 8 | PASS |
| Named indexes and unique keys | 13 | 13 | PASS |
| Data statements | 12 | 0 | PASS |

The sanitized file has no `INSERT`, `REPLACE`, `LOAD DATA`, `CREATE DATABASE`,
or `USE` statement. Its validated SHA-256 is:

```text
B57385B985474B1CE80A39C268FE5FC2D42C2E9FFD487632C915AA09169B215D
```

## Database-agnostic session configuration

The sanitized baseline no longer creates or selects a hard-coded database. It
sets the import session explicitly:

```sql
SET SQL_MODE = 'STRICT_TRANS_TABLES,NO_AUTO_VALUE_ON_ZERO';
SET time_zone = '+00:00';
```

The import target is therefore supplied by the MariaDB client rather than
embedded in the baseline.

## Controlled test database reset

The reset target was held in the exact variable value:

```text
student_document_management_test
```

Before any destructive statement, the guard required both:

- exact equality with `student_document_management_test`;
- a case-sensitive `_test` suffix.

The guard also rejected equality with `student_document_management_dev`.
Only the test database was dropped and recreated. The development database was
not selected as an operation target.

The recreated database settings were:

| Property | Observed | Result |
|---|---|---|
| Character set | `utf8mb4` | PASS |
| Collation | `utf8mb4_unicode_ci` | PASS |

## Import result

The sanitized baseline was imported using MariaDB `10.11.15` with
`student_document_management_test` supplied as the client database. The client
returned exit code `0`.

The live validation session reported:

| Property | Observed | Result |
|---|---|---|
| Database | `student_document_management_test` | PASS |
| Session timezone | `+00:00` | PASS |
| Session SQL mode | `NO_AUTO_VALUE_ON_ZERO,STRICT_TRANS_TABLES` | PASS |

## Metadata verification

| Metadata type | Observed | Result |
|---|---:|---|
| Base tables | 5 | PASS |
| Columns | 39 | PASS |
| Primary keys | 5 | PASS |
| Unique constraints | 5 | PASS |
| Foreign keys | 5 | PASS |
| CHECK constraints | 3 | PASS |
| Distinct indexes, including primary/unique | 18 | PASS |
| Triggers | 1 | PASS |

The five imported tables are:

- `document_status_history`
- `document_types`
- `students`
- `student_documents`
- `users`

All five foreign keys use `ON UPDATE CASCADE` and `ON DELETE RESTRICT`. The
three named CHECK constraints cover both `invalid_reason` invariants and the
`completed_at` invariant. The trigger
`trg_student_documents_document_code_immutable` is a `BEFORE UPDATE` trigger
on `student_documents`.

## Behavioral verification

Fake values were used inside transactions expected to fail. MariaDB correctly
rejected:

- a duplicate `document_types.code` through `uq_document_types_code`;
- a missing Student reference through
  `fk_student_documents_student_code`;
- an invalid document without `invalid_reason`;
- a completed document without `completed_at`;
- invalid history without `invalid_reason`;
- an update to an existing `document_code` through the immutable-code trigger.

Each expected rejection matched the responsible named constraint or trigger
message. Connection closure rolled back the test transaction.

## Empty-database verification

After import and behavioral checks, row counts were zero for every table:

| Table | Rows |
|---|---:|
| `document_status_history` | 0 |
| `document_types` | 0 |
| `student_documents` | 0 |
| `students` | 0 |
| `users` | 0 |

No private or fake test row remained in the database.

## Commands and checks actually run

The executed checks included:

```powershell
git check-ignore -v -- student_document_management.sql
git ls-files --error-unmatch -- student_document_management.sql
Get-FileHash student_document_management.sql -Algorithm SHA256
Get-FileHash database/schema/student_document_management_schema.sql -Algorithm SHA256
mariadb --database=student_document_management_test --execute="source ..."
```

Additional live queries inspected `information_schema.SCHEMATA`, `TABLES`,
`COLUMNS`, `TABLE_CONSTRAINTS`, `REFERENTIAL_CONSTRAINTS`,
`CHECK_CONSTRAINTS`, `STATISTICS`, and `TRIGGERS`. Expected-failure
transactions verified enforcement without persisting fixture data. No
credential or private row value was logged or committed.

## Repository changes

- `database/schema/student_document_management_schema.sql` is now
  database-agnostic and uses valid strict/UTC session settings.
- `docs/evidence/P0-03.md` records the sanitized import and verification.
- `docs/IMPLEMENTATION_PLAN.md` marks P0-03 completed.
- `.gitignore` and the private source were not changed.

## Conclusion

All P0-03 acceptance conditions are satisfied. The sanitized baseline contains
the required table, index, FK, CHECK, and trigger metadata; imports cleanly
into the dedicated MariaDB 10.11 test database; and leaves all tables empty.
P0-03 is complete. P0-04 remains incomplete, so the P0 internal exit criteria
are not yet satisfied and P1 remains blocked.
