# P1-01 — Laravel application skeleton evidence

- Task: `P1-01`
- Date: 2026-07-30
- Branch: `feature/p1-01-laravel-skeleton`
- Status: **PASS**

## Scope

This evidence records the Laravel 13.x application skeleton created at the
repository root and the application of the P0-01 runtime baseline to project
metadata. It does not install React, TypeScript, or Inertia from P1-08 and does
not configure the MariaDB application connection or session behavior from
P1-02.

## Dependency and gate check

P0-01 through P0-04 have documented completion evidence. DG-005 remains
pending, but it does not block functional implementation in P0 through P8.

## Runtime and project metadata

| Component | Required | Observed | Result |
|---|---|---|---|
| Laravel | 13.x | `13.23.0` | PASS |
| PHP | 8.4 | `8.4.23` | PASS |
| Composer | `2.10.2` | `2.10.2` | PASS |
| Node.js | `24.16.0` | `v24.16.0` | PASS |
| npm | `11.13.0` | `11.13.0` | PASS |

`composer.json` requires PHP `^8.3` and Laravel Framework `^13.8`.
`package.json#engines` pins Node.js `24.16.0` and npm `11.13.0`, while
`package.json#packageManager` is `npm@11.13.0`. The existing `.nvmrc` also
pins Node.js `24.16.0`.

## Private-data guard

The User-scope `PRIVATE_IMPORT_PATH` was verified without printing or recording
its value. The verification reported:

| Condition | Result |
|---|---|
| Variable exists | PASS |
| Value is an absolute path | PASS |
| File exists | PASS |
| File is outside the repository | PASS |

The local-mode guard inherited the verified value and returned `PASS`. The
CI-mode check removed `PRIVATE_IMPORT_PATH` from its process, confirmed it was
unset, and the guard also returned `PASS`.

The guard regression suite returned `PASS`. It covers ignored dependency files,
a forbidden staged source secret, and a forbidden staged dependency secret.

No private import path, file content, credential, SQL payload, or PII was
printed or written to this evidence.

## Application verification

The final checks produced these results:

| Check | Result |
|---|---|
| PowerShell guard parser | PASS |
| Private-data guard, local mode | PASS |
| Private-data guard, CI mode | PASS |
| Guard regression tests | PASS |
| `composer validate --no-check-publish` | PASS |
| `php artisan --version` | PASS — Laravel Framework `13.23.0` |
| `php artisan test` with a process-only temporary `APP_KEY` | PASS — 2 tests, 2 assertions |
| `npm run build` | PASS |
| `git diff --check` | PASS |
| Markdown and project-metadata consistency checks | PASS |

The sandboxed Vite attempt could not spawn a required child process and could
not load the native Tailwind binding. The required rerun outside the sandbox
completed successfully with Vite `8.1.5`. Vite emitted an informational warning
that optimized font fallbacks require the optional `fontaine` package; the
production assets were still built successfully.

## Commands actually run

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/check-private-data.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/tests/check-private-data.Tests.ps1
composer validate --no-check-publish
php artisan --version
php artisan test
npm run build
git diff --check
git status --short --branch
```

For local mode, the process received the verified User-scope environment value.
For CI mode, the process explicitly removed the variable before invoking the
same guard. The test command used a temporary process-only `APP_KEY`; no `.env`
file or secret was created or committed.

## Repository changes

- The Laravel 13.x skeleton is present at the repository root.
- `composer.json` and `composer.lock` record the PHP/Laravel dependency
  baseline.
- `package.json` and `package-lock.json` record the approved Node.js/npm
  baseline and Vite dependencies.
- `.gitignore` preserves project-generated and dependency exclusions while
  retaining the private-data policy.
- `scripts/check-private-data.ps1` ignores dependency trees during filesystem
  scanning without exempting tracked or staged forbidden files.
- `scripts/tests/check-private-data.Tests.ps1` provides regression coverage for
  that guard behavior.
- `docs/evidence/P1-01.md` records the final verification without exposing the
  private import path.
- `docs/IMPLEMENTATION_PLAN.md` marks only P1-01 completed and links this
  evidence.

## Conclusion

P1-01 satisfies its output and verification criteria: the Laravel 13.x
application boots at the repository root, the approved runtime versions are
recorded in project metadata, both guard modes pass, backend tests pass, and
the Vite production build completes successfully. The implementation commit is
`ba001ada5a5d77c33c27880ac52740daad6a01f0`.
